program test;
var x : integer;
var y : integer;
var z : integer;
begin
    read(x);
    read(y);
    z := 5;
    x := x + 1;
    y := y + z;
    while x > 0 do
        x := x - 1;
    if y > z then
        write('y is big');
    z := x + y;
    write(z);
end.